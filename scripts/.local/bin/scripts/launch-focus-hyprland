#!/bin/sh
if [ "$#" -lt 1 ]; then exit 1; fi

# Optionally pass the class in case it's not the same as the launch name
if [ $2 ]; then
	class=$2
else
	class=$1
fi

if pidof $1; then
	hyprctl dispatch focuswindow $class
else
	exec $1
fi
